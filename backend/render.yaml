services:
  - type: web
    name: ralnejj-sante-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn principal:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0